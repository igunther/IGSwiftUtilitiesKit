import XCTest
@testable import IGSwiftUtilitiesKit

final class IGSwiftUtilitiesKitTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(IGSwiftUtilitiesKit().text, "Hello, World!")
    }
}
